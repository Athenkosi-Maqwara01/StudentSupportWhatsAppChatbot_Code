# /repositories/ResponseRepository.py

from repository import  Repository
from typing import Optional, List
from Response import Response

class ResponseRepository(Repository[Response, int]):
    pass
